(function(){
  const codes={"INSPECCIÓN DE EXTINTOR":"GVC-SSH-FMT-009","INSPECCIÓN DE BOTIQUÍN":"GVC-SSH-FMT-006","INSPECCIÓN DE SIERRA DE BANCO":"GVC-SSH-FMT-005","INSPECCIÓN DE ESMERIL":"GVC-SSH-FMT-007","INSPECCIÓN DE EXTENSIÓN ELÉCTRICA":"GVC-SSH-FMT-008","LISTA DE ASISTENCIA SEMANAL":"GVC-CON-REG-036-F11","GAFETE DE IDENTIFICACIÓN":"GVC-SEG-REG-039-F01","INVESTIGACIÓN DE ACCIDENTE":"GVC-SSH-INV-001","REPORTE DE SEGURIDAD DE OBRA":"RSO"};
  function inferCode(root){const text=(root.querySelector("h1")?.textContent||"").trim().toUpperCase();return Object.entries(codes).find(([name])=>text.includes(name))?.[1]||"GVC-SSH";}
  function escapeText(value){return String(value||"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));}
  function documentTitle(root,payload){return payload.reportTitle||root.querySelector("h1")?.textContent?.replace(/\s+/g," ").trim()||"Documento SST";}
  function userName(){return window.GraviSupabase?.getProfile?.()?.full_name||window.GraviSupabase?.getUser?.()?.email||"Usuario GRAVI SST";}
  function generatedAt(){return new Date().toLocaleString("es-MX",{dateStyle:"medium",timeStyle:"short"});}
  function decorate(root,payload={}){if(!root||!root.innerHTML.trim())return;root.classList.add("corporate-document");const work=payload.work||{},active=document.querySelector("#activeWorkButton")?.textContent||"Obra activa",code=payload.code||inferCode(root),revision=payload.revision||"Rev. 01",title=documentTitle(root,payload),date=payload.date||payload.weekStart||"",signature=[code,revision,work.name||active,title,date].join("|");const currentHeader=root.querySelector(".document-corporate-header");const currentFooter=root.querySelector(".document-page-footer");if(currentFooter?.dataset.signature===signature&&currentHeader?.dataset.signature===signature)return;currentHeader?.remove();currentFooter?.remove();const header=document.createElement("header");header.className="document-corporate-header";header.dataset.signature=signature;header.innerHTML=`<div class="document-logo-box"><img src="./assets/gravi-constructora-oficial.jpeg" alt="GRAVI constructora"></div><div><b>GRAVI SST</b><h2>${escapeText(title)}</h2><p>${escapeText(work.development||"Desarrollo no especificado")} · ${escapeText(work.name||active)}</p></div><div class="document-header-meta"><span><b>Fecha</b>${escapeText(date||"Sin fecha")}</span><span><b>Responsable</b>${escapeText(userName())}</span></div>`;root.prepend(header);const footer=document.createElement("footer");footer.className="document-page-footer";footer.dataset.signature=signature;footer.innerHTML=`<span>Generado automáticamente por GRAVI SST</span><span>${escapeText(code)} · ${escapeText(revision)}</span><span>${escapeText(generatedAt())} · Página 1</span>`;root.appendChild(footer);}
  window.GvcDocuments={decorate};
  const root=document.querySelector("#printReport");if(root)new MutationObserver(()=>queueMicrotask(()=>decorate(root,window.systemDownloadData||{}))).observe(root,{childList:true});
})();
